# ── STABLE CORE ──────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2
apscheduler==3.10.4
loguru==0.7.2
fastapi==0.115.0
uvicorn==0.31.0
websockets==13.1
python-dotenv==1.0.1
asyncio-throttle==1.0.2
httpx>=0.27.0
aiohttp==3.10.5

# ── DATA & MATH ───────────────────────────────────────────
# pandas 2.2.x has pre-built wheels for Python 3.12
# pandas 2.0.x does NOT — requires source build which fails without pkg_resources
numpy>=1.26.0,<2.0.0
pandas==2.2.3
scikit-learn>=1.3.0,<1.6.0
duckdb>=0.9.0
asyncpg>=0.29.0  # PostgreSQL/TimescaleDB async driver
psutil>=5.9.0  # System and process utilities for pipeline metrics

# ── ML MODELS ────────────────────────────────────────────
xgboost>=2.0.0,<2.2.0
lightgbm>=4.0.0,<5.0.0
joblib>=1.3.0,<1.5.0
scipy>=1.11.0,<1.15.0  # Statistical functions for A/B testing
stable-baselines3>=2.0.0,<3.0.0  # Reinforcement learning (PPO agents)

# ── VECTOR STORE ─────────────────────────────────────────
chromadb>=0.4.0,<1.6.0

# ── ONNX (optional — for local inference) ────────────────
onnxruntime>=1.16.0,<1.20.0
# skl2onnx  # uncomment if using ONNX model export

# ── BROKERS ──────────────────────────────────────────────
# MetaTrader5 is Windows-only — will fail silently on Linux/Mac (Docker)
# Install manually on Windows: pip install MetaTrader5==5.0.4424
ccxt==4.3.91
alpaca-py
# ib-insync  # optional — only if using Interactive Brokers

# ── AI & FRAMEWORKS ──────────────────────────────────────
langchain==0.2.16
langgraph==0.2.28
anthropic>=0.34.2,<0.40.0
openai>=1.0.0,<2.0.0

# ── TORCH (CPU build for local dev — GPU build for training) ─
# Install separately to avoid CI/CD conflicts:
#   CPU:  pip install torch --index-url https://download.pytorch.org/whl/cpu
#   GPU:  pip install torch --index-url https://download.pytorch.org/whl/cu121
# Uncomment one line only:
# torch

# ── FEEDS ────────────────────────────────────────────────
yfinance>=0.2.40,<0.3.0  # Fixed: 0.1.91 has invalid metadata
pandas-ta>=0.4.0,<0.5.0
newsapi-python>=0.2.7
requests>=2.31.0,<3.0.0
feedparser>=6.0.0,<7.0.0

# ── MACRO / DATA COLLECTION ──────────────────────────────
fredapi>=0.5.0            # FRED API (free key at fred.stlouisfed.org)
kaggle>=1.5.0             # Kaggle API client — remote GPU fine-tuning automation
datasets>=2.14.0          # HuggingFace datasets for sentiment training data
# gdelt             # gdelt-py has Python 3.12 compatibility issues — use requests instead
