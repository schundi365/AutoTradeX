# Requirements for Reinforcement Learning Pipeline
# Install with: pip install -r ml/requirements_rl.txt

# Core RL libraries
gymnasium>=0.29.0
stable-baselines3>=2.0.0

# Dependencies (may already be installed)
numpy>=1.24.0
pandas>=2.0.0
torch>=2.0.0  # Required by stable-baselines3
